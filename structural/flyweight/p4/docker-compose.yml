version: "3.9"

services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
    env_file: .env
    environment:
      - PYTHONPATH=/app/src
    command: python -m cli.main simulate --particles 1000 --steps 20
