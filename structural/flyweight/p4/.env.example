# This project has no external configuration today, but the file is kept
# for consistency with the rest of the dataset and as a place to add
# settings (e.g. DEFAULT_PARTICLE_COUNT) if this CLI grows.
