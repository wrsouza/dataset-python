# Export Format Bridge — Environment Variables
# Copy to .env before running: cp .env.example .env

# ── Output ───────────────────────────────────────────────────────────────────
# Directory (inside the container) where exported files are written.
EXPORT_OUTPUT_DIR=./exports
