version: "3.9"

services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
    env_file: .env
    ports:
      - "8501:8501"
    command: streamlit run src/ui_components/app.py --server.port=8501 --server.address=0.0.0.0
