# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# S3 / LocalStack
AWS_ENDPOINT_URL=http://localstack:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=cloud-adapter-bucket

# GCS (fake — no real credentials needed)
GCS_PROJECT=fake-project
GCS_BUCKET=cloud-adapter-gcs

# Azure (fake — no real credentials needed)
AZURE_ACCOUNT_NAME=fakeaccount
AZURE_ACCOUNT_KEY=fakekey
AZURE_CONTAINER=cloud-adapter-azure
