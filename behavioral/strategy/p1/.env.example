# PostgreSQL connection used by SQLAlchemy
DATABASE_URL=postgresql://app:secret@db:5432/taxdb

# PostgreSQL container settings (docker-compose)
POSTGRES_USER=app
POSTGRES_PASSWORD=secret
POSTGRES_DB=taxdb
