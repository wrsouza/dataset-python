version: "3.9"

services:

  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
    env_file: .env
    ports:
      - "5000:5000"
    depends_on:
      mysql:
        condition: service_healthy
    command: flask --app src/discount_strategy_api/app.py run --host 0.0.0.0 --port 5000 --debug

  mysql:
    image: mysql:8
    environment:
      MYSQL_DATABASE: discount_strategy_api
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10
