# Dashboard Component Mediator — variáveis de ambiente
# Não há banco de dados ou serviço externo; o mediador vive em memória
# dentro do próprio processo Streamlit, em st.session_state.

STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
