# PostgreSQL connection used by the app service (see docker-compose.yml)
POSTGRES_USER=app
POSTGRES_PASSWORD=secret
POSTGRES_DB=appdb

# SQLAlchemy connection string consumed by document_editor.infrastructure.database
DATABASE_URL=postgresql+psycopg2://app:secret@db:5432/appdb
