# Data Validation Pipeline — variáveis de ambiente
# Não há banco de dados ou serviço externo; a cadeia de validação vive em
# memória dentro do próprio processo Streamlit.

STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
