# PostgreSQL connection used by PostgresImportRepository
DATABASE_URL=postgresql://app:secret@db:5432/appdb

# PostgreSQL container settings (docker-compose)
POSTGRES_USER=app
POSTGRES_PASSWORD=secret
POSTGRES_DB=appdb
