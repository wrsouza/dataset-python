# ── Serializer Factory — Environment Variables ────────────────────────────────
# Copy to .env and adjust as needed. This project has no external services;
# only Streamlit server settings are configurable.

STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
